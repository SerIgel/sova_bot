const { admin } = require("../config.json")
// This is command for one-time actions (i.e. API tests)
module.exports = {
    name: 'execute',
    guildOnly: true,
    description: 'order 66',
    async execute(message, args) {
    },
};